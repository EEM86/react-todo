import React from "react"
import './app-header.css'

const AppHeader = () => {
    return (
        <span className="app-header">
            <h1>My Todo List</h1>
        </span>
    );
};

export default AppHeader;